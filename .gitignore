# Excel lock/temp files (created while a workbook is open)
~$*.xlsx
.~lock.*

# macOS
.DS_Store

# Raw/unprocessed logo uploads — only the cropped, optimized versions in
# assets/logos/ are used by the site and committed.
/Logos/
